input_list = [1, 2, 8, 9, 4, 1, 2]

# Calculate the maximum value in the input list
max_value = max(input_list)

# Find the missing elements by comparing the full range with the input list
missing_elements = [num for num in range(1,max_value+1) if num not in input_list]

print("Missing elements:", missing_elements)

missing_string = ', '.join(map(str, missing_elements))

print("Missing elements:", missing_string)


output:
Missing elements: [3, 5, 6, 7]
Missing elements: 3, 5, 6, 7
